import * as React from 'react';
import { shallow } from 'enzyme';

import Experience from './Experience';

describe('Experience', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<Experience {...defaultProps} />);
    });
});
